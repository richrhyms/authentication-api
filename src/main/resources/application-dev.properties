spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/authentication_api_db
spring.datasource.username=postgres
spring.datasource.password=postgres
#SEQUENCE_DEFINITION_SQL_QUERY=DO $$ BEGIN CREATE SEQUENCE %s; EXCEPTION WHEN duplicate_table THEN END $$ LANGUAGE plpgsql;
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:5MB}
spring.servlet.multipart.max-request-size=${MAX_FILE_REQUEST_SIZE:5MB}
spring.jpa.hibernate.ddl-auto=update
#springdoc.swagger-ui.tagsSorter=alpha
#springdoc.swagger-ui.filter=true